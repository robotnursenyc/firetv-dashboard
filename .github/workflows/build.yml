name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # ── Android SDK — use pre-installed /opt/android-sdk directly ────────────
      # ubuntu-22.04 ships Android SDK at /opt/android-sdk.
      # No third-party actions needed.
      - name: Set Android SDK environment
        run: |
          export ANDROID_HOME=/opt/android-sdk
          export ANDROID_SDK_ROOT=/opt/android-sdk
          echo "ANDROID_HOME=$ANDROID_HOME" >> $GITHUB_ENV
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "SDK contents: $(ls $ANDROID_HOME/)"
          echo "platforms: $(ls $ANDROID_HOME/platforms/ 2>/dev/null || echo 'none')"
          echo "build-tools: $(ls $ANDROID_HOME/build-tools/ 2>/dev/null || echo 'none')"

      # Install required SDK components if they're missing.
      - name: Install Android SDK components
        run: |
          export ANDROID_HOME=/opt/android-sdk
          export ANDROID_SDK_ROOT=/opt/android-sdk
          if [ -d "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" ]; then
            yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses 2>&1 | tail -3
            $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --install \
              "platforms;android-34" \
              "platforms;android-26" \
              "build-tools;34.0.0" 2>&1 | tail -5
          else
            echo "sdkmanager not found — using pre-installed SDK as-is"
            echo "Platforms available: $(ls $ANDROID_HOME/platforms/ 2>/dev/null)"
            echo "Build-tools available: $(ls $ANDROID_HOME/build-tools/ 2>/dev/null)"
          fi

      - name: Download Gradle
        run: |
          cd /tmp
          if [ ! -f "gradle-8.4/bin/gradle" ]; then
            wget -q "https://services.gradle.org/distributions/gradle-8.4-bin.zip" -O gradle.zip
            unzip -q gradle.zip
          fi
          echo "/tmp/gradle-8.4/bin" >> $GITHUB_PATH

      # Create local.properties so Gradle knows where the Android SDK lives.
      # ANDROID_HOME is set in the env block of the Build step, but the
      # Android Gradle plugin also reads local.properties.
      - name: Create local.properties
        run: |
          echo "sdk.dir=/opt/android-sdk" > $GITHUB_WORKSPACE/local.properties
          echo "Created: $(cat $GITHUB_WORKSPACE/local.properties)"

      # ── Inject secrets into gradle.properties and acra.properties before build ────
      # Auth token: used in BuildConfig for X-App-Auth header.
      # Telegram creds: used in acra.properties for crash reporting.
      - name: Inject secrets for release builds
        run: |
          # gradle.properties — auth token + versionCode from run_number
          echo "dashboardAuthToken=${{ secrets.DASHBOARD_AUTH_TOKEN }}" >> $GITHUB_WORKSPACE/gradle.properties
          echo "CI_BUILD_NUMBER=${{ github.run_number }}" >> $GITHUB_WORKSPACE/gradle.properties
          echo "Auth token + build number written to gradle.properties"

          # acra.properties — Telegram crash reporting credentials
          # ACRA 5.x correct property names (lowercase, dot-separated).
          # These are NOT hardcoded in source; CI injects them here.
          cat > $GITHUB_WORKSPACE/app/src/main/assets/acra.properties << 'ACRA_EOF'
          acra.enable=true
          acra.reportFormat=JSON
          acra.unsafeDeviceEncryption=false
          acra.telegram.enabled=true
          acra.telegram.botToken=${{ secrets.TELEGRAM_BOT_TOKEN }}
          acra.telegram.chatId=${{ secrets.TELEGRAM_CHAT_ID }}
          ACRA_EOF
          echo "acra.properties written (Telegram credentials injected)"

      - name: Build release APK
        continue-on-error: true
        env:
          ANDROID_HOME: /opt/android-sdk
          ANDROID_SDK_ROOT: /opt/android-sdk
        run: |
          echo "=== Java version ==="
          java -version 2>&1
          echo "=== Gradle version ==="
          gradle --version 2>&1
          echo "=== ANDROID_HOME ==="
          echo "ANDROID_HOME=$ANDROID_HOME"
          echo "SDK contents: $(ls $ANDROID_HOME/)"
          echo "platforms: $(ls $ANDROID_HOME/platforms/ 2>/dev/null || echo none)"
          echo "build-tools: $(ls $ANDROID_HOME/build-tools/ 2>/dev/null || echo none)"
          echo "=== Working directory ==="
          echo "PWD=$(pwd)"
          echo "=== Running assembleRelease ==="
          gradle :app:assembleRelease --no-daemon --stacktrace 2>&1
          echo "=== APK files found ==="
          find . -name "*.apk" 2>/dev/null

      # ── Diagnostic snapshot — always runs so we can see the full state ────────
      - name: Diagnose build state
        run: |
          echo "=== app/build/ directory ==="
          ls -laR app/build/ 2>/dev/null | head -80 || echo "no app/build dir"
          echo "=== find *.apk anywhere ==="
          find . -name "*.apk" 2>/dev/null | head -20
          echo "=== gradle.properties (auth token redacted) ==="
          sed 's/dashboardAuthToken=.*/dashboardAuthToken=REDACTED/' gradle.properties 2>/dev/null
          echo "=== local.properties ==="
          cat local.properties 2>/dev/null || echo "none"

      - name: Verify release APK was produced
        run: |
          APK="$GITHUB_WORKSPACE/app/build/outputs/apk/release/app-release.apk"
          if [ ! -f "$APK" ]; then
            echo "ERROR: app-release.apk not found!"
            echo "APK dir contents:"
            ls -la $GITHUB_WORKSPACE/app/build/outputs/apk/ 2>/dev/null || echo 'apk dir does not exist'
            ls -la $GITHUB_WORKSPACE/app/build/outputs/ 2>/dev/null || echo 'outputs dir does not exist'
            exit 1
          fi
          echo "APK size: $(du -h "$APK" | cut -f1)"

      # ── Sign v2-only, strip v1 ─────────────────────────────────────────────
      # Uses a dedicated UPLOAD_KEYSTORE (not the debug keystore) to avoid
      # the "dual-signing with debug key" issue on older FireTV hardware.
      # The keystore + password come from GitHub Secrets.
      # Note: openssl-generated PKCS12 keystores use alias "1" by default.
      # If signing fails with "No key found", set UPLOAD_KEYSTORE_ALIAS=1 in secrets.
      - name: Strip v1 signature and re-sign v2-only
        env:
          UPLOAD_KEYSTORE_B64: ${{ secrets.UPLOAD_KEYSTORE_B64 }}
          UPLOAD_KEYSTORE_ALIAS: ${{ secrets.UPLOAD_KEYSTORE_ALIAS }}
          UPLOAD_KEYSTORE_PASSWORD: ${{ secrets.UPLOAD_KEYSTORE_PASSWORD }}
          UPLOAD_KEY_PASSWORD: ${{ secrets.UPLOAD_KEY_PASSWORD }}
          ANDROID_HOME: /opt/android-sdk
          ANDROID_SDK_ROOT: /opt/android-sdk
        run: |
          echo "=== Decoding keystore ==="
          echo "$UPLOAD_KEYSTORE_B64" | base64 -d > /tmp/firetv-upload.jks
          echo "Keystore decoded, size: $(du -h /tmp/firetv-upload.jks | cut -f1)"

          # Show available aliases so we know exactly what key name to use.
          echo "=== Available keys in keystore ==="
          keytool -list -v -keystore /tmp/firetv-upload.jks \
            -storepass "$UPLOAD_KEYSTORE_PASSWORD" 2>&1 | grep -A2 "Alias name" || true

          APK_IN="$GITHUB_WORKSPACE/app/build/outputs/apk/release/app-release.apk"
          APK_FIXED="/tmp/firetv-signed.apk"

          echo "=== Verifying input APK ==="
          $ANDROID_HOME/build-tools/34.0.0/apksigner verify -v --min-sdk-version 26 "$APK_IN" 2>&1 || true

          echo "=== Re-signing v2-only (strip v1) with alias '$UPLOAD_KEYSTORE_ALIAS' ==="
          $ANDROID_HOME/build-tools/34.0.0/apksigner sign \
            --v1-signing-enabled false \
            --v2-signing-enabled true \
            --ks /tmp/firetv-upload.jks \
            --ks-key-alias "$UPLOAD_KEYSTORE_ALIAS" \
            --ks-pass "pass:$UPLOAD_KEYSTORE_PASSWORD" \
            --key-pass "pass:$UPLOAD_KEY_PASSWORD" \
            --out "$APK_FIXED" \
            "$APK_IN" 2>&1

          echo "=== Verifying output signature ==="
          $ANDROID_HOME/build-tools/34.0.0/apksigner verify -v --min-sdk-version 26 "$APK_FIXED" 2>&1

          VERIFY_OUT=$($ANDROID_HOME/build-tools/34.0.0/apksigner verify -v --min-sdk-version 26 "$APK_FIXED" 2>&1)
          if echo "$VERIFY_OUT" | grep -q "Verified using v1 scheme (JAR signing)"; then
            echo "ERROR: v1 signature still present!"
            exit 1
          fi
          if echo "$VERIFY_OUT" | grep -q "Verified using v2 scheme"; then
            echo "SUCCESS: APK is signed v2-only (FireTV compatible)"
          fi

          echo "=== Copying signed APK back ==="
          cp "$APK_FIXED" "$APK_IN"
          echo "Final APK: $(du -h "$APK_IN" | cut -f1)"

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: firetv-release-aligned
          path: ${{ github.workspace }}/app/build/outputs/apk/release/app-release.apk
          retention-days: 7

      # ── Deploy to VPS ────────────────────────────────────────────────────────
      - name: Deploy to VPS
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          # Write SSH key to disk (0600 required)
          echo "$VPS_SSH_KEY" > /tmp/vps_key
          chmod 600 /tmp/vps_key

          BUILD_NUM=${GITHUB_RUN_NUMBER:-0}

          echo "=== Deploying firetv-release.apk (build #${BUILD_NUM}) ==="

          scp -o StrictHostKeyChecking=no -i /tmp/vps_key \
            "$GITHUB_WORKSPACE/app/build/outputs/apk/release/app-release.apk" \
            $VPS_USER@$VPS_HOST:/tmp/firetv-release.apk

          ssh -o StrictHostKeyChecking=no -i /tmp/vps_key $VPS_USER@$VPS_HOST \
            "docker cp /tmp/firetv-release.apk apk-serve:/usr/share/nginx/html/apk/firetv-release.apk && \
             docker exec apk-serve sh -c 'chmod 644 /usr/share/nginx/html/apk/firetv-release.apk' && \
             docker exec apk-serve sh -c 'echo \"{\\\"version\\\":\\\"1.0\\\",\\\"build\\\":${BUILD_NUM},\\\"apk_url\\\":\\\"https://dashboard.cashlabnyc.com/apk/firetv-release.apk\\\"}\" > /usr/share/nginx/html/apk/version.json' && \
             echo VPS deploy complete"

      # ── Notify on success ─────────────────────────────────────────────────────
      - name: Notify Telegram on success
        if: success()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d "text=✅ FireTV APK deployed to VPS. Build %23${{ github.run_number }}. URL: https://dashboard.cashlabnyc.com/apk/firetv-release.apk" \
            2>&1 | tail -3

      - name: Notify Telegram on failure
        if: failure()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d "text=❌ FireTV APK build FAILED. Run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
            2>&1 | tail -3

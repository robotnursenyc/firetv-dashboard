# ACRA 5.x configuration template.
# This file must NOT contain real secrets in source.
#
# CI replaces placeholder values with real secrets if needed.
#
# ACRA is only active in release builds (isDebuggable=false in buildType).

acra.enable=true
acra.reportFormat=JSON
acra.unsafeDeviceEncryption=false

<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_background">

    <!-- WebView fills the entire screen -->
    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Semi-transparent overlay so the WebView content shows through slightly -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#40000000" />

    <!-- Exit Button 1: On-screen X button, top-right corner -->
    <FrameLayout
        android:id="@+id/exitButtonContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_margin="24dp"
        android:clickable="true"
        android:focusable="true"
        android:padding="12dp">

        <!-- Semi-transparent rounded background -->
        <View
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/exit_button_bg" />

        <!-- X symbol -->
        <TextView
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:gravity="center"
            android:text="×"
            android:textColor="#FFFFFFFF"
            android:textSize="32sp"
            android:textStyle="bold" />

    </FrameLayout>

    <!-- Exit Button 2: Invisible touch target covering bottom-right -->
    <View
        android:id="@+id/exitBottomRight"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:background="@android:color/transparent"
        android:clickable="true"
        android:focusable="true" />

</FrameLayout>
